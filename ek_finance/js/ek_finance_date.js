jQuery(
  function() {
    jQuery( '#edit-from' ).datepicker({ dateFormat: 'yy-mm-dd' } );	
    jQuery( '#edit-to' ).datepicker({ dateFormat: 'yy-mm-dd' } );
  	jQuery( '.date' ).datepicker({ dateFormat: 'yy-mm-dd' } );
  
  });	